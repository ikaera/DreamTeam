const Employee = require('../lib/employee');

descripbe('', () => {
  it('', () => {
    expect().toEqual();
  });
});

descripbe('', () => {
  it('', () => {
    expect().toEqual();
  });
});

descripbe('', () => {
  it('', () => {
    expect().toEqual();
  });
});
